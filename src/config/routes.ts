import HomePage from '../pages/home/Home';

const routes = {
  public: [
    {
      name: 'Home',
      path: '/',
      exact: true,
      component: HomePage
    },
  ],
  private: [
     
  ]
}

export default routes;